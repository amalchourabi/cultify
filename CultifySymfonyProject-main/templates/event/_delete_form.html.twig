<form method="post" action="{{ path('app_event_delete', {'id_e': event.getIdE}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.getIdE) }}">
    <button class="btn">Delete</button>
</form>
