<form method="post" action="{{ path('app_reservation_delete', {'id_r': reservation.getIdR}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.getIdR) }}">
    <button class="btn">Delete</button>
</form>
